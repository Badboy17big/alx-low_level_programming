#include "main.h"

/**
 * main -  Entry point
 *
 * descreption: using sizeof to print the size of various types on the computer
 *
 * Return: always 0 (success)
*/

int main(void)
{
	char str[] = "_putchar";
	int x;

	for (x = 0; ch < 8; ch++)
		_putchar(str[x]);
	_putchar('\n');
	return (0);
}
